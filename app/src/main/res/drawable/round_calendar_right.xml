<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- view background color -->
    <solid
        android:color="@android:color/white" >
    </solid>

    <!-- view border color and width -->
    <stroke
        android:width="@dimen/_1sdp"
        android:color="@android:color/white" >
    </stroke>

    <!-- If you want to add some padding -->


    <!-- Here is the corner radius -->
    <corners
        android:topLeftRadius="@dimen/cal_right_top_left_radius"
        android:bottomLeftRadius="@dimen/cal_right_bottom_left_radius"
        android:topRightRadius="@dimen/cal_right_top_right_radius"
        android:bottomRightRadius="@dimen/cal_right_bottom_right_radius"
        >
    </corners>

</shape>